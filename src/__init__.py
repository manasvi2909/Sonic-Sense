"""
Music Intelligence System
=========================
Context-aware, emotionally aligned, personalized music recommendation engine.

Modules:
    content_model       — Content-based engine (audio feature NN search)
    mood_engine         — Mood/context mapping & mood-cluster assignment
    user_model          — User behavioral profile modeling
    autoencoder         — Deep embedding upgrade via autoencoder
    hybrid_recommender  — Multi-signal hybrid scoring engine
    evaluation          — Advanced evaluation metrics (ILD, novelty, coverage…)
    clustering_viz      — Clustering & 2-D visualization
    explainability      — Per-recommendation natural-language explanations
    api_integration     — Spotify Web API scaffold
"""

__version__ = "2.0.0"
